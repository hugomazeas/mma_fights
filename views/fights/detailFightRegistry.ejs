<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fight Details</title>
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">
</head>
<body>
    <!-- Modal Component -->
    <div class="fixed inset-0 z-10 overflow-y-auto">
        <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
            <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"></div>

            <!-- Modal panel -->
            <div class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
                <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                    <div class="sm:flex sm:items-start">
                        <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left w-full">
                            <div class="mt-2">
                                <div class="grid grid-cols-2 gap-y-3 text-base text-gray-600">
                                    <!-- Fighter Information -->
                                    <div class="col-span-2 font-bold text-center text-gray-800 py-3 text-xl">Fighter Details</div>
                                    <div class="text-right font-semibold mr-3">Fighter 1:</div>
                                    <div class="<%= fight.winner_full_name === fight.fighter1_full_name ? 'text-green-600' : 'text-red-600' %>"><strong><%= fight.fighter1_full_name %></strong></div>
                        
                                    <div class="text-right font-semibold mr-3">Fighter 2:</div>
                                    <div class="<%= fight.winner_full_name === fight.fighter2_full_name ? 'text-green-600' : 'text-red-600' %>"><strong><%= fight.fighter2_full_name %></strong></div>
                        
                                    <!-- Event Information -->
                                    <div class="col-span-2 font-bold text-center text-gray-800 py-3">Event Information</div>
                                    <div class="text-right font-semibold mr-3">Event:</div>
                                    <div><%= fight.event_name %></div>
                        
                                    <div class="text-right font-semibold mr-3">UFC Number:</div>
                                    <div><%= fight.ufc_number %></div>
                        
                                    <div class="text-right font-semibold mr-3">Card Type:</div>
                                    <div><%= fight.card_type_name %></div>
                        
                                    <!-- Round Information -->
                                    <div class="col-span-2 font-bold text-center text-gray-800 py-3">Round Information</div>
                                    <div class="text-right font-semibold mr-3">Number of Rounds:</div>
                                    <div><%= fight.number_round %></div>
                        
                                    <div class="text-right font-semibold mr-3">Round Length:</div>
                                    <div><%= fight.round_length %></div>
                                </div>
                            </div>
                        </div>
                        
                    </div>
                </div>
                <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
                    <button type="button" class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-red-600 text-base font-medium text-white hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 sm:ml-3 sm:w-auto sm:text-sm">
                        Close
                    </button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
